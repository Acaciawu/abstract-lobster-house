services:
  - type: web
    name: abstract-lobster-house-live
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_RELEASE
        value: svg-lobster-modal-v2
